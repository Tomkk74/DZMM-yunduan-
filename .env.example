# Copy to .env and fill in (or use the web console login to write automatically)
email=
password=
# cookie=  # generated after login; usually no need to edit

# Optional overrides (also configurable via config.json / web UI)
# character_id=0
# project_path=D:\path\to\your-game
# origin=https://www.aifukk.com
# 国内线路见 https://dzmm-home.github.io/dzmm-addr/ ；换 origin 后需重新登录
